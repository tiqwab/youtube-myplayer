import React from 'react';

class MusicFormAddBtn extends React.Component {
  render() {
    return (
      <button
        id="btn-add"
        className="btn"
      >Add</button>
    );
  }
}

export default MusicFormAddBtn;
