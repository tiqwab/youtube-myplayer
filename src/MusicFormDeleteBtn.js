import React from 'react';

class MusicFormDeleteBtn extends React.Component {
  render() {
    return (
      <button id="btn-delete" className="btn">Delete</button>
    );
  }
}

export default MusicFormDeleteBtn;
